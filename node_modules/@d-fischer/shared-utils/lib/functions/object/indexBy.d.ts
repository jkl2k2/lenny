import { KeyMapper } from '../../types/object';
export default function indexBy<T>(arr: T[], key: Extract<keyof T, string>): Record<string, T>;
export default function indexBy<T>(arr: T[], keyFn: KeyMapper<T>): Record<string, T>;
