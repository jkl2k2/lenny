import { ISODuration } from '../types';
export declare function parseUrl(url: string): {
    video: string;
    playlist: string;
    channel: string;
};
export declare function parseIsoDuration(duration: string): ISODuration;
